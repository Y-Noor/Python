LOG IN FORM (v1)

To do:

---> Implement access to certain files or partitions only if logged in successfully.

---> Implement encryption algorithm for the passwords before storing them in the file.

---> Create a GUI for the form. 